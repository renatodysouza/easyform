version: 2.1
jobs:
  test:
    docker:
      - image: 'ubuntu'
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - run: cd ../workspace && ng test
workflows:
  version: 2
  build_and_test:
    jobs:
      - test